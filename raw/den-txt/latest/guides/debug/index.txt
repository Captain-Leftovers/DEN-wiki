    Skip to content

   den
   (BUTTON) Search CtrlK
   (BUTTON) Cancel
   Sponsor Den [den?logo=github&color=white] Ask DeepWiki GitHub
   Select theme [Auto_]
   (BUTTON)
     * Den
     * Learn
     * Templates
     * Cookbook
     * Reference

     * Overview
     * Motivation
     * Versioning
     * Community
     * Contributing

     * Core Principles
     * Context System
     * Aspects & Functors
     * Parametric Aspects
     * NixOS Context Pipeline
     * Library vs Framework

     * Overview
     * Minimal
     * Default
     * No-Flake
     * NVF Standalone
     * MicroVM
     * Example
     * Bug Reproduction
     * CI Tests
     * Flake Parts Modules

     * From Zero to Den
     * From Flake to Den
     * Declare Hosts & Users
     * Configure Aspects
     * Host<->User Mutual Config
     * Custom Nix Classes
     * Homes Integration
     * Use Batteries
     * Share with Namespaces
     * Angle Brackets Syntax
     * Migrate to Den
     * Debug Configurations

     * den.schema
     * den.aspects
     * den.provides
     * den.ctx
     * den.lib
     * flake.*

   Sponsor Den [den?logo=github&color=white] Ask DeepWiki GitHub
   Select theme [Auto_]
   On this page
     * Overview
     * Den CI REPL
     * Your Flake REPL Inspection
     * Expose den for Inspection
     * Trace Aspect Includes
     * Trace Context
     * Break into REPL
     * Manually Resolve an Aspect
     * Inspect a Host’s Main Module
     * Common Issues

On this page

     * Overview
     * Den CI REPL
     * Your Flake REPL Inspection
     * Expose den for Inspection
     * Trace Aspect Includes
     * Trace Context
     * Break into REPL
     * Manually Resolve an Aspect
     * Inspect a Host’s Main Module
     * Common Issues

vic/den?

   Become a Sponsor

   Den is part of @vic's Dendritic Ecosystem — made with love and
   maintained with passion.
     * import-tree
     * flake-aspects
     * flake-file
     * with-inputs
     * denful
     * dendrix

   More @vic OpenSource

Debug Configurations

Den CI REPL

   Section titled “Den CI REPL”

   The following loads denTest and den.lib at REPL for exploration.
   Terminal window

   just repl

   (BUTTON)

   This will not load your project definitions, for that use:

Your Flake REPL Inspection

   Section titled “Your Flake REPL Inspection”

   Load your flake and explore interactively:
   Terminal window

   $ nix repl

   nix-repl> :lf .
   nix-repl> nixosConfigurations.igloo.config.networking.hostName
   "igloo"
   (BUTTON)

Expose den for Inspection

   Section titled “Expose den for Inspection”

   Temporarily expose the den attrset as a flake output:

   { den, ... }: {

   flake.den = den; # remove after debugging
   }
   (BUTTON)

   Then in REPL:
   Terminal window

   nix-repl> :lf .

   nix-repl> den.aspects.igloo
   nix-repl> den.hosts.x86_64-linux.igloo
   nix-repl> den.ctx
   (BUTTON)

Trace Aspect Includes

   Section titled “Trace Aspect Includes”

   Use the following example code to get a trace (list of nested
   aspect.name) of included aspects.

   let

   inherit (den.lib.aspects) resolve adapters;
   aspect = den.hosts.x86_64-linux.resolved;
   in
   resolve.withAdapter adapters.trace "nixos" aspect
   (BUTTON)

Trace Context

   Section titled “Trace Context”

   Print context values during evaluation:

   den.aspects.laptop.includes = [

   ({ host, ... }@ctx: builtins.trace ctx {
   nixos.networking.hostName = host.hostName;
   })
   ];
   (BUTTON)

Break into REPL

   Section titled “Break into REPL”

   Drop into a REPL at any evaluation point:

   den.aspects.laptop.includes = [

   ({ host, ... }@ctx: builtins.break ctx {
   nixos = { };
   })
   ];
   (BUTTON)

Manually Resolve an Aspect

   Section titled “Manually Resolve an Aspect”

   Test how an aspect resolves for a specific class:

   Terminal window

   nix-repl> module = den.lib.aspects.resolve "nixos" []
   den.aspects.laptop;

   nix-repl> config = (lib.evalModules { modules = [ module ]; }).config
   (BUTTON)

   For parametric aspects, apply context first:

   Terminal window

   nix-repl> aspect = den.aspects.laptop { host =
   den.hosts.x86_64-linux.laptop; }

   nix-repl> module = den.lib.aspects.resolve "nixos" [] aspect;
   (BUTTON)

Inspect a Host’s Main Module

   Section titled “Inspect a Host’s Main Module”

   Terminal window

   nix-repl> module = den.hosts.x86_64-linux.igloo.mainModule

   nix-repl> cfg = (lib.nixosSystem { modules = [ module ]; }).config
   nix-repl> cfg.networking.hostName
   (BUTTON)

Common Issues

   Section titled “Common Issues”

   Duplicate values in lists: Den deduplicates owned and static configs
   from den.default, but parametric functions in den.default.includes run
   at every context stage. Use den.lib.perHost to restrict:

   den.lib.perHost ({ host }: { nixos.x = 1; })

   (BUTTON)

   Missing attribute: The context does not have the expected parameter.
   Trace context keys to see what is available.

   Wrong class: Check that host.class matches what you expect. Darwin
   hosts have class = "darwin", not "nixos".

   Module not found: Ensure the file is under modules/ and not prefixed
   with _ (excluded by import-tree).

   Edit page

   Previous
   Migrate to Den Next
   den.schema

   Contribute Community Sponsor
